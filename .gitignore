# Python
__pycache__/
*.py[cod]
*.pyo

# Secrets
config/secrets.json

# Runtime config (copy config.example.json → config.json on new installs)
config/config.json

# Runtime / logs
workdammit.log
*.log

# Runtime cache and data (updated every run, not source-controlled)
.cache/
data/

# Mac dev stub for rgbmatrix C extension (must NOT be deployed to Pi)
rgbmatrix/

# Editor / OS
.DS_Store
.claude/settings.local.json

# Downloaded team logos (cached at runtime)
assets/sports/logos/*/
